<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/stylesheet/style.css" />
</head>

<body>
  <div class="mainPage">
    <header>
      <h1>
        <a href="/" id="headerh1">Tech Blog</a>
      </h1>
       <nav class="nav-class">
          {{#if loggedIn}}
          <span class="span-class">logged in!</span>
          <div class="div-nav">
            <a href="/dashboard">Dashboard</a>
            <button id="logout" class="btn-no-style">logout</button>
          </div>
          {{else}}
          <span class="span-class">Please login</span>
          <div class="div-nav">
            <a href="/login">Login</a>
            <a href="/signup">Signup</a>
          </div>
          {{/if}}
        </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
      Thanks for visiting!
    </footer>
  </div>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}

</body>

</html>